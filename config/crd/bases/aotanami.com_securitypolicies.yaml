---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: securitypolicies.aotanami.com
spec:
  group: aotanami.com
  names:
    kind: SecurityPolicy
    listKind: SecurityPolicyList
    plural: securitypolicies
    singular: securitypolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.severity
      name: Severity
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.violationCount
      name: Violations
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityPolicy is the Schema for the securitypolicies API.
          It defines security rules to evaluate and enforce on Kubernetes workloads.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SecurityPolicy
            properties:
              autoRemediate:
                default: false
                description: |-
                  autoRemediate enables automatic fix generation via GitOps for violations found by this policy.
                  Only effective when a GitOpsRepository is onboarded (Protect Mode).
                type: boolean
              match:
                description: match defines the scope of this policy — which namespaces
                  and workloads it applies to.
                properties:
                  excludeNamespaces:
                    description: excludeNamespaces excludes specific namespaces from
                      evaluation.
                    items:
                      type: string
                    type: array
                  labelSelector:
                    description: labelSelector restricts the policy to resources matching
                      these labels.
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  namespaces:
                    description: namespaces restricts the policy to specific namespaces.
                      Empty means all namespaces.
                    items:
                      type: string
                    type: array
                  resourceKinds:
                    description: resourceKinds restricts the policy to specific resource
                      kinds (e.g., Deployment, StatefulSet, Pod).
                    items:
                      type: string
                    type: array
                type: object
              notificationChannels:
                description: notificationChannels lists the names of NotificationChannel
                  resources to send alerts to.
                items:
                  type: string
                type: array
              rules:
                description: rules defines the set of security rules to evaluate.
                items:
                  description: SecurityRule defines a single security check to perform.
                  properties:
                    enforce:
                      default: true
                      description: enforce when true, the rule violations are treated
                        as failures; when false, as warnings.
                      type: boolean
                    name:
                      description: name is a unique identifier for this rule within
                        the policy.
                      type: string
                    params:
                      additionalProperties:
                        type: string
                      description: params provides rule-specific configuration parameters.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type:
                      description: type identifies the category of security check.
                      enum:
                      - container-security-context
                      - rbac-audit
                      - image-vulnerability
                      - network-policy
                      - pod-security
                      - secrets-exposure
                      - resource-limits
                      - privilege-escalation
                      type: string
                  required:
                  - name
                  - type
                  type: object
                minItems: 1
                type: array
              schedule:
                description: schedule defines a cron expression for periodic evaluation.
                  If empty, the policy is evaluated continuously.
                type: string
              severity:
                default: medium
                description: severity defines the minimum severity level of findings
                  to report.
                enum:
                - critical
                - high
                - medium
                - low
                - info
                type: string
            required:
            - match
            - rules
            - severity
            type: object
          status:
            description: status defines the observed state of SecurityPolicy
            properties:
              conditions:
                description: conditions represent the current state of the SecurityPolicy
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastEvaluated:
                description: lastEvaluated is the timestamp of the last policy evaluation.
                format: date-time
                type: string
              phase:
                description: phase indicates the current lifecycle phase of the policy.
                enum:
                - Pending
                - Active
                - Error
                type: string
              violationCount:
                description: violationCount is the total number of active violations
                  found by this policy.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
