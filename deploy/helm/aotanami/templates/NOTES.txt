🚀 Aotanami has been deployed!

{{- if .Values.config.dashboard.enabled }}

📊 Dashboard:
  Access the Aotanami dashboard:

  kubectl port-forward svc/{{ include "aotanami.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }} -n {{ .Release.Namespace }}

  Then open: http://localhost:{{ .Values.service.port }}{{ .Values.config.dashboard.basePath }}
{{- end }}

⚙️  Operating Mode: {{ .Values.config.mode | upper }}
{{- if eq .Values.config.mode "audit" }}
   → Aotanami is in Audit Mode (alert-only). No cluster modifications will be made.
   → To enable Protect Mode, onboard a GitOps repository with a GitOpsRepository CRD.
{{- else }}
   → Aotanami is in Protect Mode. It will create PRs for detected issues.
{{- end }}

🔧 Next Steps:
  1. Apply a SecurityPolicy:
     kubectl apply -f https://raw.githubusercontent.com/aotanami/aotanami/main/config/samples/aotanami_v1alpha1_securitypolicy.yaml

  2. Check operator logs:
     kubectl logs -f deploy/{{ include "aotanami.fullname" . }} -n {{ .Release.Namespace }}

  3. View CRDs:
     kubectl get securitypolicies,clusterscans,scanreports -A

📖 Documentation: https://github.com/aotanami/aotanami/tree/main/docs
